# 元星木可运行证据

这里把不同范围的验证分开保存，不把核心脚本测试算作整个 Agent 的防御结果。

| 入口 | 验证范围 | 结果 |
| --- | --- | --- |
| [核心运行](verified-core-report.json) | 实际 Linux 沙箱、任务家族状态、重连、broker 重启、权限撤销、独立收件端 | 19 个检查通过；2 次内部发送与 1 次独立公开发送。也记录了无害公开摘要被保守拦截的代价。 |
| [OpenClaw](openclaw/README.md) | 2026.9.4 原生 CLI、原生 exec provider、三阶段任务与会话恢复；本地脚本模型 | 有明确覆盖范围的真实宿主运行，未评测强模型攻击。 |
| [Hermes](hermes/README.md) | v2026.9.7 官方插件注册、原生文件/终端工具、环境重建、撤销 | 实际工具与执行环境运行；未运行 LLM Agent 循环或原生子代理委派。 |

[旧的 MCP 接入实验](../../LEGACY-MCP.zh-CN.md)属于前一阶段，与这套执行核心不是同一件事；未完成的框架案例不能计为验证通过。

`live_console.py` 是固定合成场景的本地录制面板后端。它不接受任意命令、文件路径、任务 ID 或外部目标，不是生产管理 API。每个按钮调用真实 broker / worker 或原生框架探针，录制时保留新的完整执行记录。
