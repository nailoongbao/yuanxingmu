# 命令检查修复：看清真正要执行的操作

0.7.0a2 源码修复了 Issues [7](https://github.com/yh-l20/yuanxingmu/issues/7)、[8](https://github.com/yh-l20/yuanxingmu/issues/8)、[9](https://github.com/yh-l20/yuanxingmu/issues/9) 报告的命令规则遗漏。[配套 `0.4.0a2` 安装器](https://github.com/yh-l20/yuanxingmu/releases/tag/installer-0.4.0a2)已发布；按[安装步骤](https://yh-l20.github.io/yuanxingmu/start.html#install)使用新的 `~/yuanxingmu-v07a2` 目录。已经发布的安装器 `0.4.0a1` 固定使用运行包 `0.7.0a1`，不包含这些修复，也不会自动更新。

有些命令会先调整优先级、设置环境变量或等待一段时间，再运行真正的操作。原来的检查在读取部分选项时，可能把选项值当成程序名，让后面的危险操作漏过检查。另一个问题是把 `echo`、`printf` 一律当成打印文字，忽略了它们也能向文件写入内容。

## 改变了什么

| 情况 | 新的处理 |
| --- | --- |
| `nice`、`ionice`、`env`、`timeout`、`time` 后面还有命令 | 识别支持的选项及其值，继续检查真正的操作。 |
| `xargs`、`parallel` 间接运行命令 | 已能确定的危险操作阻止；程序或操作对象仍不确定时要求复核。 |
| `echo`、`printf` 通过重定向写入敏感文件 | 检查实际写入目标，不再按普通打印放行。 |
| 仅把敏感路径当文字打印 | 保留正常打印，不因出现路径名称就阻止。 |
| `env` 改变目录后使用相对路径 | 无法确定实际访问目标时要求复核；直接固定打印有有限例外。 |
| 多行文字输入、注释、环境变量赋值 | 保留仍会执行的指令和敏感变量引用，区分普通正文与实际命令替换。 |

正常、可确定的操作继续执行。这里只把无法判断的间接操作交回复核，不把“没解析出来”视为安全。

## 怎样核对

所有 Issue 样例都只作为字符串传入检查函数，没有执行样例命令、访问样例网络地址或读取真实凭据。它们证明的是规则检查遗漏；本次没有演示沙箱逃逸。

最终独立审核的提交为 `492feb097363e64e291018740122285736579b1a`，已保留在仓库历史中。运行代码与测试文件分别是 [guards.py](../yuanxingmu/guards.py) 和 [间接命令回归](../tests/test_yuanxingmu_indirect_commands.py)。审核时两文件的 SHA256：

```text
4ee050b0416796d375662540cd60f4c8a5472c5c68b25f5511ec171e7a603d55  yuanxingmu/guards.py
a41829cb53638cd7831c231c9809289646ed3124532507c400c59eca9aed6f1b  tests/test_yuanxingmu_indirect_commands.py
```

| 最终提交完整测试 | 通过 | 跳过 | 失败 |
| --- | ---: | ---: | ---: |
| Windows / Python 3.12 | 361 | 648 | 0 |
| WSL Ubuntu / Python 3.12 | 807 | 202 | 0 |

跳过来自平台和可选依赖条件，不计为通过。独立审查另外核对了 36 条定向样例、92 条字符串对照，以及 23 项 Linux 输入与技能测试；这是有限回归范围，不是攻击阻断率。

本轮修复也保留了之前遗漏的反例：注释里的假多行输入标记不能吞掉下一行指令，未引用多行输入中的命令替换仍需检查。引用的结束标记、普通正文和仅打印文字均有正常用法对照。

## 范围

这是有限语法的检查，不是完整 shell 解释器。未知选项、过深的嵌套和不能确定的动态执行仍可能要求复核；部分正常嵌套打印也会进入复核。输入与技能层对自然语言的指令识别也有范围限制，不能据此声称所有措辞都被检查。实际权限与隔离仍由宿主的独立边界执行。

已发布的 `0.7.0a2` 运行包仍有动词后直接接冒号的前缀漏检。后续 [PR #10](https://github.com/yh-l20/yuanxingmu/pull/10) 已在主分支修复 `Run:`、`Execute:`、`Invoke:` 及全角冒号形式，并保留无冒号用法、否定与引用的正常对照。这项后续修复尚未进入 `0.4.0a2` 安装器固定下载的运行包；已发布文件保持原样。

官网 OpenClaw PROTECTED14、Hermes AUTO19 视频继续标记其真实运行源码 `c83add3` / `0.7.0a1`。它们没有重录，不能作为本次命令解析修复的实机验证。
