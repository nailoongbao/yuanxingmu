# 2026-09-12 本地整合检查

本轮覆盖分层设置、创建基线恢复、Hermes 执行上下文修复、OpenClaw 状态命令、回答审查及技能用途关联。它们是组件和接口检查，真实模型、官方网页和发送结果见各自的原生报告。

| 检查 | 实际结果 |
|---|---|
| Windows 全量 Python | 779 项：213 通过，566 跳过，无失败或错误。 |
| Linux/WSL 首轮全量 Python | 779 项：591 通过，187 跳过，1 项失败。 |
| Linux 失败模块修正后 | 11/11 通过，无跳过，运行时源码未再变更。 |

唯一失败是独立检查模型的设置集成测试仍使用旧保存请求，没有携带读取到的策略摘要。产品按新契约拒绝保存，未写入设置。修正测试为实际 GET 后提交版本摘要，保留密钥、固定配置、任务身份及工作台重开的断言；没有放宽产品校验。

两次 Linux 检查之间只修改了这一测试文件。完整套件没有为这个测试数据修正重复运行；这里明确保留首轮失败和相关模块复验，不能把它们写成一次全量运行全部通过。源码在各次运行前后的哈希一致。

跳过项目主要依赖额外 SDK、官方 Hermes 环境或 Linux 执行条件。它们不是成功项，也不代表已做对应框架的真实模型验证。完整源码摘要、跳过原因和日志摘要见 [verification.json](verification.json)。

另构建了本地源码预览 wheel，85 份运行文件在源码、wheel 和独立安装目录中逐字节一致；全部 RECORD 摘要通过。脱离源码导入路径的 `desk`、`hermes init`、`defense` 帮助入口均正常。见 [wheel-verification.json](wheel-verification.json)。这是本地开发构建，虽然版本号仍为 `0.5.0a1`，它的 SHA-256 与旧公开包不同，不能拿这个文件名冒称公开安装器已更新。首轮元数据检查使用了不兼容 Windows CRLF 的字符串匹配，改用标准邮件头解析器后对同一个 wheel 继续核验，没有重建或更换包。
