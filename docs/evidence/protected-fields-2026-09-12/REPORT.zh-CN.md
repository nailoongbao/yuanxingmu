# 0.7 开发版：敏感字段防护检查

固定源码：`c83add3ef0aea291bc7a7dbbb1b7a7f11acdb425`。这是开发候选，公开 0.6 运行包不包含新边界。

| 环境 | 总计 | 通过 | 跳过 | 失败 |
|---|---:|---:|---:|---:|
| 本机 WSL Ubuntu / Python 3.12 | 984 | 782 | 202 | 0 |
| 本机 Windows / Python 3.12 | 984 | 337 | 647 | 0 |

跳过项包含平台限制和未安装的可选框架环境，不能计作通过。运行前后源码清单未变。153 份受记录文件中，151 份与提交逐字节一致；两份既有 JSON fixture 仅换行符不同，解析后的 JSON 相同，差异哈希完整保留在 `source-fixture-byte-note.json`。新增/修改的 27 份提交文件与受测工作区逐字节一致。

同一提交的 GitHub Checks `34693379873`、执行隔离 `34693379849` 均为首次运行成功；各 job 保存在 `ci-verified.json`，包含两平台 core、五组原生工具适配检查和执行隔离。它们不代表每个框架都具备 Hermes/OpenClaw 的宿主模型网关边界。

本次覆盖资料遮盖、固定来源及篡改拒绝、JSON 转义/重复键、金额有限变体、子任务与重开、关闭五层后的独立保护、完整 SSE 工具参数、发往模型之前检查、检查模型解释的泄露、动作/邮件实际提交前检查以及故障拒绝。合成接收器只在本机，邮件传输被替换为测试函数。

另有 20 份 AUTO17/AUTO18 的原始 GLM 返回离线回放，全部能按支持的协议解析。AUTO18 的 00193 和 00198 两份底价泄露被确定性检查拦截；原失败样本没有修改。回放没有发模型请求，不能冒充新的实机验证。

敏感字段不是通用秘密识别器：只覆盖登记的固定标签、有限归一化和同次返回内重组，不保证任意编码、推理或跨请求拆分。原生 Hermes 的 AUTO19 结果需另看独立记录。

离线核对本目录：`python verify.py`。重跑源码检查：检出上面的固定提交，运行 `python -m unittest discover -s tests -q`；平台和可选依赖会影响跳过数。
